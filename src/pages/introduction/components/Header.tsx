const headerStyle = {
  minWidth: "64px",
  lineHeight: "32px",
  padding: "0 16px",
  color: "#fff",
  background: "#639",
};

function Header() {
  return <header style={headerStyle}>head</header>;
}

export default Header;
